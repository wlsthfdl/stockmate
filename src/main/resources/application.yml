spring:
  application:
    name: stockmate

  datasource:
    url: jdbc:mysql://localhost:3306/stockmate?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: stockmate
    password: stockmate1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

server:
  port: 8080

jwt:
  secret: stockmate-secrect-key-for-jwt-token-generation-please-change-this-in-production
  expiration: 86400000      # 24시간(밀리초 단위)